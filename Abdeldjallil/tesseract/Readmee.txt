1-you should install the following packages:
 tesseract from its web site and the librery to use it in python pytesseract
 in addition you need to install the following packages:
pip install opencv-python pillow pytesseract numpy python-docx
pip install opencv-python
pip install pillow
pip install pytesseract
pip install numpy
pip install python-docx


2- you need to convert the pdf file to images (png) to use this code: